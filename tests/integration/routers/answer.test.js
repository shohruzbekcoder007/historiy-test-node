const request = require('supertest')
let server;

describe('/api/v1/answer', () => {
    // beforeEach(() => {
    //     server = require('./../../../index')
    // })
    // afterEach(() => {
    //     server.close()
    // })
    // describe('GET /', () => {
        it('afteer learn testing post method', () => {
            expect(2).toBe(2)
        })
    // })
})